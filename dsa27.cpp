# include <iostream> 
using namespace std ;
int main() {
    int n  , a[20];
    int m = a.size() ;
    cout<< "enter the array :" ;
    for (int i = 0 ; i<a.size() ; i++){
        cin>>a[i] ;
    }
    for(int i = 0 ; i<a.size() ; i++){
        for(int j = i+1 ; j<a.size() ; j++ ){
            if (a[i] == a[j]){
                

            }
        }
    }
}